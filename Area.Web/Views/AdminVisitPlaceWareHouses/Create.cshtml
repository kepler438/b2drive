@model Area.Data.VisitPlaceWareHouse

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="span12">
    <div class="widget ">
        <div class="widget-header">
            <i class="icon-user"></i>
            <h3>-</h3>
        </div>
        <div class="widget-content">
            <div class="tabbable">
                <div class="tab-content">
                    <div class="tab-pane active" id="formcontrols">
                        @using (Html.BeginForm("Create", "AdminVisitPlaceWareHouses", FormMethod.Post, new { @id = "edit-profile", @class = "form-horizontal" }))
    {

                    <div class="control-group">
                        <label class="control-label" for="quantity">Depo</label>
                        <div class="controls">
                            @Html.DropDownListFor(model => model.WareHouseID, ViewData["VisitPlaceID"] as List<SelectListItem>, "Seçiniz...")
                            <input type="hidden" name="VisitPlaceID" value="@Url.RequestContext.RouteData.Values["id"]" />
                        </div>
                    </div>

                    <div class="control-group">
                        <label class="control-label" for="note">Durumu</label>
                        <div class="controls">
                            @Html.EditorFor(model => model.IsActive)
                        </div>
                    </div>

                    <a href="/adminsupervisorvisit" class="form-control">Listeye Geri Dön</a>
                    <button type="submit" class="btn btn-primary pull-right two">Kaydet</button>
}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>